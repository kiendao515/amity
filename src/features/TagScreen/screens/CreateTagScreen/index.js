import React from 'react';
import PropTypes from 'prop-types';
import BaseLayout from 'general/components/BaseLayout';

CreateTagScreen.propTypes = {
    
};

function CreateTagScreen(props) {
    return (
        <BaseLayout>
            create tag
        </BaseLayout>
    );
}

export default CreateTagScreen;