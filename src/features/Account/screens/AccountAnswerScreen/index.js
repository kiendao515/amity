import React from 'react';
import PropTypes from 'prop-types';

AccountAnswerScreen.propTypes = {
    
};

function AccountAnswerScreen(props) {
    return (
        <div>
            
        </div>
    );
}

export default AccountAnswerScreen;